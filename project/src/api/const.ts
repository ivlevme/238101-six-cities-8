export enum APIRoute {
  Comments = '/comments',
  Fovorite = '/favorite',
  Hotels = '/hotels',
  Login = '/login',
  Logout = '/logout',
  Nearby = '/nearby',
  Status = '/status'
}

export enum HttpCode {
  Unauthorized = 401,
}
